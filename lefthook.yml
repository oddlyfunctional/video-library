pre-commit:
  parallel: true
  jobs:
    - run: npx eslint --fix "{staged_files}"
      glob:
        - "*.ts"
        - "*.tsx"
      stage_fixed: true

    - run: npx prettier --write "{staged_files}"
      glob:
        - "*.ts"
        - "*.tsx"
      stage_fixed: true
